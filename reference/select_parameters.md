# Select parameters from a list by name or index

Select parameters from a list by name or index

## Usage

``` r
select_parameters(all_list, pars)
```

## Arguments

- all_list:

  A named list of parameters.

- pars:

  A character or numeric vector.

## Value

A subset of the list.
