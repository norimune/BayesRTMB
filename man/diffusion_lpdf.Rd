% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{diffusion_lpdf}
\alias{diffusion_lpdf}
\title{Diffusion model log-probability density function}
\usage{
diffusion_lpdf(x, response, alpha, tau, beta, delta, K_diff = 20, sum = TRUE)
}
\arguments{
\item{x}{Response times. A vector, or a matrix with units in rows and trials in columns.}

\item{response}{Binary responses with the same shape as \code{x}. Positive values indicate the upper boundary.}

\item{alpha}{Boundary separation. A scalar, row-wise vector, trial-wise vector, or matrix compatible with \code{x}.}

\item{tau}{Non-decision time. A scalar, row-wise vector, trial-wise vector, or matrix compatible with \code{x}.}

\item{beta}{Initial bias as a proportion of the boundary separation. A scalar, row-wise vector, trial-wise vector, or matrix compatible with \code{x}.}

\item{delta}{Drift rate. A scalar, row-wise vector, trial-wise vector, or matrix compatible with \code{x}.}

\item{K_diff}{Number of terms in the truncated infinite-series approximation.}

\item{sum}{Logical; if \code{TRUE} (default), returns the summed log-likelihood.}
}
\value{
The summed log-likelihood, row-wise log-likelihoods for matrix input, or observation-wise log-likelihoods for vector input.
}
\description{
Log-likelihood for a two-boundary drift diffusion model. The first argument is
the response time and the second argument is the binary response. Positive
response values are treated as upper-boundary responses; zero or negative
values are treated as lower-boundary responses.

If \code{x} is a matrix, rows are treated as independent units and columns as
repeated trials. In that case, \code{sum = FALSE} returns one log-likelihood
contribution per row. If \code{x} is a vector, \code{sum = FALSE} returns one
contribution per observation.
}
