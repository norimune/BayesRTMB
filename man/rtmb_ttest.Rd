% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_ttest.R
\name{rtmb_ttest}
\alias{rtmb_ttest}
\title{RTMB-based Bayesian two-sample t-test wrapper function}
\usage{
rtmb_ttest(
  x,
  y = NULL,
  data = NULL,
  r = 0.707,
  paired = FALSE,
  ID = NULL,
  y_range = NULL,
  prior = prior_flat(),
  init = NULL,
  fixed = NULL,
  var.equal = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector of responses for group 1, a formula (e.g., `y ~ group`), or a column name (unquoted) if `data` is provided.}

\item{y}{Numeric vector of responses for group 2, or a column name (unquoted) if `data` is provided. Required if `x` is not a formula.}

\item{data}{Data frame containing the variables.}

\item{r}{Numeric; Cauchy prior scale for the effect size (delta). Default is 0.707.}

\item{paired}{Logical; whether to perform a paired t-test.}

\item{ID}{Character; name of the ID variable for paired t-tests (required for formula input with paired = TRUE).}

\item{y_range}{Theoretical minimum and maximum values of the response variable as a vector c(min, max). Required when using weakly informative priors.}

\item{prior}{An object of class `"rtmb_prior"`.
Use `prior_flat()` for no prior, `prior_normal()` for default normal/exponential priors,
or `prior_weak()` for weakly informative Bayesian inference.
Default is `prior_flat()`.}

\item{init}{List of initial values.}

\item{fixed}{Optional named list of fixed values for specific parameters.}

\item{var.equal}{Logical; whether to assume equal variances. Default is TRUE.}

\item{...}{Additional arguments.}
}
\value{
An `RTMB_Model` object.
}
\description{
Performs a Bayesian or Frequentist two-sample t-test using RTMB.
}
