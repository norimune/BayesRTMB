% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_functions.R
\name{rtmb_loglinear}
\alias{rtmb_loglinear}
\title{RTMB-based Log-linear Analysis (Contingency Table)}
\usage{
rtmb_loglinear(formula, data, prior = prior_uniform(), fixed = NULL, ...)
}
\arguments{
\item{formula}{A formula describing the table structure (e.g., `~ A + B` for independence, `~ A * B` for dependence).}

\item{data}{A data frame, table, or matrix.}

\item{prior}{Prior specification (e.g., `prior_uniform()` or `prior_weak()`). Default is `prior_uniform()`.}

\item{...}{Additional arguments passed to the internal estimation engine.}

\item{classic}{Logical; if TRUE, perform frequentist estimation and ANOVA tests.}

\item{fisher}{Logical; if TRUE, perform Fisher's exact test (for 2D tables).}
}
\value{
An `RTMB_Model`, `MCMC_Fit`, or `Classic_Fit` object depending on the settings.
}
\description{
`rtmb_loglinear` performs log-linear analysis of contingency tables using RTMB.
It models cell frequencies using a Poisson distribution and a log link function.
For Bayesian inference, it supports weakly informative priors (Stan-style).
For frequentist inference (`classic = TRUE`), it provides Wald-based ANOVA for independence tests.
}
\examples{
\donttest{
# 2x2 table analysis
df <- as.data.frame(Titanic)
fit <- rtmb_loglinear(Survived ~ Sex + Age, data = df, classic = TRUE)
fit$anova()

# Bayesian 4-way table interaction analysis
fit_bayas <- rtmb_loglinear(~ Class * Sex * Age * Survived, data = Titanic, prior = prior_weak())
}
}
