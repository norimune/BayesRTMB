% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_mediation.R
\name{rtmb_mediation}
\alias{rtmb_mediation}
\title{RTMB-based Mediation Analysis Wrapper}
\usage{
rtmb_mediation(
  formula,
  data,
  family = "gaussian",
  prior = prior_uniform(),
  y_range = NULL,
  fixed = NULL,
  view = NULL,
  ...
)
}
\arguments{
\item{formula}{A list of formulas defining the regression paths (e.g., `list(M ~ X, Y ~ X + M)`).}

\item{data}{A data frame containing the variables.}

\item{family}{A single character string or a list of character strings specifying the error distribution
for each equation (e.g., `family = list("gaussian", "binomial")`). Default is "gaussian".}

\item{prior}{An object of class "rtmb_prior" specifying the prior distribution.}

\item{y_range}{Theoretical minimum and maximum values of the response variable.}

\item{fixed}{A named list of parameter values to fix (optional).}

\item{view}{Character vector of parameter names to prioritize in summary.}

\item{...}{Additional arguments passed to the model construction.}
}
\value{
An `RTMB_Model` object.
}
\description{
`rtmb_mediation` performs mediation analysis by simultaneously estimating multiple
GLM regression equations. It automatically identifies mediation paths and calculates
indirect, direct, and total effects.
}
\details{
The function identifies mediation paths by looking for variables that are
responses in one equation and predictors in another. Indirect effects are
calculated as the product of coefficients along these paths ($a \times b$).

\strong{Uncertainty Estimation}:
When using `$optimize(ci_method = "sampling")`, the function provides asymmetric
confidence intervals for indirect effects based on the distribution of products,
which is more accurate than the standard Sobel test (Delta Method).
}
