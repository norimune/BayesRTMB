% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_mdu.R
\name{rtmb_mdu}
\alias{rtmb_mdu}
\title{RTMB-based Multidimensional Unfolding Wrapper}
\usage{
rtmb_mdu(
  data,
  ndim = 2,
  distance = c("squared", "euclidean"),
  alpha = c("random", "fix"),
  method = c("rating", "Best", "Best-Worst"),
  sets = NULL,
  prior = prior_flat(),
  y_range = NULL,
  init = NULL,
  fixed = NULL,
  view = NULL,
  distance_eps = 1e-04
)
}
\arguments{
\item{data}{Numeric matrix or data frame (N rows x M items) for
`method = "rating"`. For choice methods, a list containing `Best` and,
for `method = "Best-Worst"`, `Worst`. For `method = "Best-Worst"`, a
single matrix/data frame is treated as pre-coded `Y_dif` pair indices.}

\item{ndim}{Number of unfolding dimensions.}

\item{distance}{Character; `"squared"` uses squared Euclidean distance
(default, often easier for optimization), while `"euclidean"` uses Euclidean
distance.}

\item{alpha}{Character; `"random"` estimates item-specific alpha values as
random effects (default), while `"fix"` estimates a single common alpha.}

\item{method}{Character; `"rating"` for continuous ratings, `"Best"` for
best-only choice tasks, or `"Best-Worst"` for best-worst choice tasks.}

\item{sets}{Matrix or data frame of presented item sets (P tasks x C items)
for choice methods.}

\item{prior}{Prior configuration: `prior_flat()`, `prior_normal()`, or
`prior_weak()`. `prior_flat()` creates a maximum-likelihood model suitable
for `classic()`. The latent coordinates `delta` and `theta` are always
treated as random effects with normal scale priors, similarly to IRT ability
parameters.}

\item{y_range}{Optional response range. If supplied with the default flat
prior, `prior_weak()` is used.}

\item{init}{Optional named list of initial values.}

\item{fixed}{Optional named list of parameter values to fix.}

\item{view}{Character vector of parameter names to prioritize in summaries.}

\item{distance_eps}{Small positive constant added to the distance.}
}
\value{
An `RTMB_Model` object.
}
\description{
Fits a multidimensional unfolding model for preference/rating data. Rows are
persons or observations and columns are stimuli/items. The model represents
both row scores (`theta`) and item locations (`delta`) in a shared D-dimensional
space.
}
